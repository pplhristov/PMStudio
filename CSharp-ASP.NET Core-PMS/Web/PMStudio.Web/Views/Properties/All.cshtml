@model PropertiesListViewModel
@{
    this.ViewData["Title"] = "All Properties";
}

<h1>@this.ViewData["Title"]</h1>
<hr />
<div class="col-md-12 text-center">
    <a class="btn btn-primary" asp-area="" asp-controller="Properties" asp-action="Create">Add New Property</a>
</div>
<hr />
<div class="row">
    @foreach (var property in Model.Properties)
    {
        <img src="..." class="align-self-center mr-3" alt="...">
        <div class="media-body">
            <h5 class="mt-0"><a asp-action="ById" asp-route-id="@property.Id"> @property.Name</a></h5>
            @property.Address
        </div>
    }
</div>
<hr />
<partial name="_PagingPartial" model="@Model" />
